create table kpimodel(
    kpiModellId         varchar(50) primary key,
    kpiModelCode        varchar(20),
    kpiModelName        varchar(20),
    kpiModelType        varchar(20),
    kpiModelHtml        varchar(2000),
    kpiModelJs          varchar(2000),
    kpiModelData        varchar(2000)
)